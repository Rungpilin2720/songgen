# 🚀 คำสั่ง Push โค้ดไปยัง GitHub

## หลังจากสร้าง repository บน GitHub แล้ว ให้รันคำสั่งนี้:

```bash
git push -u origin main
```

## ถ้าเจอ error ให้ลองคำสั่งนี้:

```bash
# ตรวจสอบ remote
git remote -v

# ถ้า remote ไม่ถูกต้อง ให้แก้ไข
git remote set-url origin https://github.com/Rungpilin2720/songgen-web.git

# Push อีกครั้ง
git push -u origin main
```

## หลังจาก Push สำเร็จ:

1. ไปที่ https://github.com/Rungpilin2720/songgen-web
2. ตรวจสอบว่าไฟล์ทั้งหมดถูก push ขึ้นไปแล้ว
3. ไปที่ https://share.streamlit.io/ เพื่อ deploy
4. เลือก repository songgen-web
5. Deploy ได้เลย!

## 🎯 ผลลัพธ์:
- ได้ URL: https://songgen-web.streamlit.app
- คนอื่นสามารถใช้งาน SongGen ได้ 